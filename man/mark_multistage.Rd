% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mark_multistage.R
\name{mark_multistage}
\alias{mark_multistage}
\title{Mark Multistage Hierarchical Units and Generate Perturbation Flags}
\usage{
mark_multistage(data, ids = NULL, strata = NULL)
}
\arguments{
\item{data}{A data table or data frame containing sampling records.
For example, `data = survey_data`.}

\item{ids}{A formula or character vector defining the multistage cluster variables.
Variables must be ordered from the first sampling stage to the last.
For example, `ids = ~ PSUid + SSUid` or
`ids = c("PSUid", "SSUid")`.}

\item{strata}{A formula or character vector indicating the stratification variables.
Use `NULL` if the sampling design is not stratified.
For example, `strata = ~ Year + Sector + Area + Quarter`,
`strata = c("Year", "Sector", "Area", "Quarter")`, or
`strata = NULL`.}
}
\value{
A modified data table containing original data and delta flag columns by stratum.
}
\description{
Prepares the dataset for the rescaled bootstrap method by validating cluster
variables and generating random hierarchical perturbation indicators (delta flags)
across multistage sampling levels.
}
\examples{
\dontrun{
Generate a reproducible synthetic two-stage fishery sample
fishery_data <- generate_fishery_data(
  seed = 123,
  trip_range = c(15, 20),
  operation_range = c(3, 5)
)

mark_multistage(
  data   = fishery_data,
  ids    = ~ FTid + FOid,
  strata = ~ Year + Fleet + FishingArea + Quarter
)
}

}
